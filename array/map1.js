/**
 * Executa um certo processamento
 * em todos os dados de um array
 * gerando um novo array
 */

const nuns = [1,2,3,4,5]

let resultado = nuns.map(conteudo => conteudo * 2)

console.log(resultado)